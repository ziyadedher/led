[package]
name = "rp1-pio"
version.workspace = true
edition = "2021"
description = "Minimal userspace driver for the Raspberry Pi 5 RP1 PIO block (/dev/pio0)."

[dependencies]
nix = { version = "0.29", features = ["ioctl"] }
libc = "0.2"
thiserror = "1.0"
